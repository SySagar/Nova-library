import React from 'react';
 
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  const activeContext = React.createContext({active:false,setActive:()=>{},setDeactive:()=>{}});

 
export default activeContext;