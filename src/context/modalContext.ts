import React from 'react';
 
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  const modalContext = React.createContext({active:false,setActive:()=>{},setDeactive:()=>{}});

 
export default modalContext;